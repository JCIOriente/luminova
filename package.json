{
  "name": "luminova",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "concurrently -k -n emu,web -c blue,green \"bash tools/scripts/emulators.sh\" \"wait-on tcp:127.0.0.1:4010 tcp:127.0.0.1:4030 && pnpm seed:emulator && turbo run dev\"",
    "build": "turbo run build",
    "build:local": "VITE_FIREBASE_EMULATOR_ENABLED=true turbo run build --filter spotlight --filter backstage",
    "preview:local": "pnpm build:local && concurrently -k -n emu,app -c blue,green \"bash tools/scripts/emulators.sh\" \"wait-on tcp:127.0.0.1:4010 tcp:127.0.0.1:4030 && pnpm seed:emulator && turbo run preview --filter spotlight --filter backstage\"",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "ci": "turbo run ci",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "knip": "knip",
    "audit": "pnpm audit --audit-level=high",
    "test:seed": "node --test 'tools/scripts/lib/**/*.test.mjs'",
    "test:harness": "node --test \".claude/hooks/**/*.test.mjs\"",
    "pr-tests": "pnpm format && turbo run ci && pnpm knip && pnpm audit --audit-level=high && pnpm test:seed && pnpm test:harness",
    "seed:emulator": "FIRESTORE_EMULATOR_HOST=127.0.0.1:4010 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:4030 node tools/scripts/seed-emulator.mjs",
    "seed:production": "node tools/scripts/seed-production.mjs",
    "deploy:rules": "firebase deploy --only firestore,storage",
    "deploy:indexes": "firebase deploy --only firestore:indexes",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "VITE_FIREBASE_EMULATOR_ENABLED=false turbo run build --filter spotlight --filter backstage && firebase deploy --only hosting",
    "deploy:all": "pnpm deploy:rules && pnpm deploy:functions && pnpm deploy:hosting"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.3",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "firebase-admin": "13.10.0",
    "knip": "^6.15.0",
    "prettier": "^3.8.3",
    "turbo": "^2.9.16",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.1",
    "vitest": "^4.1.8",
    "wait-on": "^9.0.10"
  }
}
