{
  "name": "@luminova/firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:run": "vitest run",
    "test": "bash ../../tools/scripts/with-emulator-lock.sh firebase emulators:exec --only firestore --project demo-rules-test \"pnpm run test:run\"",
    "ci": "pnpm run test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@luminova/types": "workspace:*",
    "@types/node": "^25.9.1",
    "firebase": "12.14.0",
    "vitest": "^4.1.8"
  }
}
